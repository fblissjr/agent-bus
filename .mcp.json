{
  "mcpServers": {
    "agent-bus": {
      "type": "http",
      "url": "${AGENT_BUS_URL:-http://127.0.0.1:8765}/mcp",
      "headers": {
        "Authorization": "Bearer ${AGENT_BUS_TOKEN}"
      }
    }
  }
}
